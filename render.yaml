services:
  - type: web
    name: concall-research-tool
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
    healthCheckPath: /
    autoDeploy: true